import numpy as np

# value for the classification into the different relative position
REL_DIR_ANGLE = np.sqrt(2) / 2

# pourcentage of relative direction observation necessary to classify
REL_DIR_MIN_PERC = 0.75
